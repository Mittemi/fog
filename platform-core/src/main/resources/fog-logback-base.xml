<included>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty scope="context" name="serverId" source="fog.server.id"/>
    <springProperty scope="context" name="serviceId" source="fog.service.id"/>

    <!--<contextName>${serverId}</contextName>-->

    <springProfile name="!local">
        <include resource="fog-logback-base-file.xml"/>
    </springProfile>
    <springProfile name="local">
        <include resource="fog-logback-base-console.xml"/>
    </springProfile>

    <logger name="com.example.demo" level="INFO"/>

    <logger name="org.springframework.boot.SpringApplication" level="INFO"/>

    <!--<springProfile name="!local">-->
    <!--<logger name="AccessLog" level="TRACE" additivity="false">-->
    <!--<appender-ref ref="JSON"/>-->
    <!--</logger>-->
    <!--</springProfile>-->

</included>
